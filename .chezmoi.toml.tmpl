# Chezmoi config - auto-generated during init
# Profiles: workstation (main/away), server (automation), minimal (hb)

[data]
{{- $hostname := .chezmoi.hostname }}
{{- $profile := "workstation" }}
{{- $username := .chezmoi.username }}

{{/* Detect profile based on hostname patterns */}}
{{- $hostLower := lower $hostname }}
{{- if or (contains "homebridge" $hostLower) (contains "hb" $hostLower) }}
{{-   $profile = "minimal" }}
{{- else if or (eq $hostname "M1") (contains "automation" $hostLower) }}
{{-   $profile = "server" }}
{{- end }}

    hostname = {{ $hostname | quote }}
    profile = {{ $profile | quote }}
    username = {{ $username | quote }}
